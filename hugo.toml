baseURL = 'https://kerrigan.jhax.dev/'
languageCode = 'en-us'
title = 'Kerrigan'
enableRobotsTXT = true
pluralizelisttitles = false
disableKinds = ["taxonomy", "term"]

[params]
  description = "AI-powered session transcription and clinical note generation for behavioral health — running entirely on your hardware. No patient data in the cloud."
  tagline = "Clinical AI That Stays On Your Premises"
  formspreeID = "mwvreeno"
  twitterHandle = "@CoalescentEm"
  ogImage = "/img/og-kerrigan.png"

  # Analytics (opt-in; disabled by default until keys are set)
  analyticsEnabled = false
  posthogKey = ""
  posthogHost = "https://us.i.posthog.com"
  clarityProjectId = ""
  ga4MeasurementId = ""

[markup.goldmark.renderer]
  unsafe = true
